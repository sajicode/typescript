import { calculateRectangle } from './math/rectangle';
import { PI, calculateCircumference } from './math/circle';

console.log(calculateRectangle(4, 5));
console.log(PI);
console.log(calculateCircumference(6));
